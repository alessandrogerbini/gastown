package internal

import "testing"

// TestPolecatLifecycleSmoke is a trivial smoke test to verify the
// polecat sling→execute→shutdown lifecycle works end-to-end (gt-71w).
func TestPolecatLifecycleSmoke(t *testing.T) {
	t.Log("polecat lifecycle smoke test: ok")
}
